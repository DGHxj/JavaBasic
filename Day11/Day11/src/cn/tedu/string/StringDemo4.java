package cn.tedu.string;

public class StringDemo4 {

	public static void main(String[] args) {

		// String str = "sdgaiodos";

		// 获取指定的字符在字符串中第一次出现的下标
		// 如果找不到则返回-1
		// System.out.println(str.indexOf('s'));
		// System.out.println(str.indexOf('p'));
		// 从指定的下标开始，向后获取指定的字符在字符串中第一次出现的下标
		// System.out.println(str.indexOf('s', 5));
		// System.out.println(str.indexOf("sa", 5));
		// 获取指定的字符最后一次出现的位置
		// System.out.println(str.lastIndexOf("s"));
		// 从指定的下标开始向前寻找元素最后一次出现的位置
		// System.out.println(str.lastIndexOf("s", 5));

		// String str1 = new String("abc");
		// String str2 = "abc";
		// intern表示强制返回这个字符串在常量池的地址
		// System.out.println(str1.intern() == str2);

		// String str = "";
		// 判断字符串是否是一个空字符串
		// System.out.println(str.isEmpty());

	}

}
